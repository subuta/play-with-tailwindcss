.button {
  @apply py-2 px-4 rounded-lg;

  &:focus {
    @apply outline-none;
  }
}

.filledButton {
  @apply button bg-gray-300;
}

.ghostButton {
  @apply button border border-gray-300;

  &:hover {
    @apply bg-gray-300;
  }
}
